name: authentication_repository
description: authorize
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_auth: ^1.0.1
  firebase_core: ^1.0.1
  freezed_annotation: ^0.14.1
  google_sign_in: ^5.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: null
  freezed: ^0.14.1+1
  mockito: ^4.0.0
